---
interface Props {
  title: string;
  description?: string;
  href: string;
  imageUrl?: string;
  timeAgo?: string;
  category?: string;
  readingTime?: string;
  variant?: "default" | "hero" | "sidebar" | "compact";
}

const {
  title,
  description,
  href,
  imageUrl,
  timeAgo,
  category,
  readingTime,
  variant = "default",
} = Astro.props;
---

<article class:list={["card", `card--${variant}`]}>
  <a href={href} class="card-link">
    {imageUrl && (
      <div class="card-image">
        <img src={imageUrl} alt={title} loading="lazy" />
      </div>
    )}
    <div class="card-body">
      <h3 class="card-title">{title}</h3>
      {description && <p class="card-desc">{description}</p>}
      {(timeAgo || category) && (
        <div class="card-meta">
          {timeAgo && <time class="card-time">{timeAgo}</time>}
          {timeAgo && category && <span class="card-divider" aria-hidden="true">|</span>}
          {category && <span class="card-category">{category}</span>}
          {readingTime && <><span class="card-divider" aria-hidden="true">|</span><span class="card-reading-time">{readingTime}</span></>}
        </div>
      )}
    </div>
  </a>
</article>

<style>
  .card-link {
    display: block;
    color: inherit;
    text-decoration: none;
  }

  .card-link:hover {
    text-decoration: none;
  }

  .card-link:hover .card-title {
    text-decoration: underline;
  }

  .card-image {
    overflow: hidden;
    margin-bottom: 14px;
  }

  .card-image img {
    width: 100%;
    aspect-ratio: 16 / 9;
    object-fit: cover;
    transition: transform 0.2s;
  }

  .card-link:hover .card-image img {
    transform: scale(1.02);
  }

  .card-title {
    font-family: var(--font-serif);
    font-size: 1.2rem;
    font-weight: 700;
    line-height: 1.3;
    margin-bottom: 8px;
    color: var(--color-text);
    letter-spacing: -0.01em;
  }

  .card-desc {
    font-size: 0.9rem;
    line-height: 1.6;
    color: var(--color-text-secondary);
    margin-bottom: 10px;
  }

  .card-meta {
    font-size: 0.8rem;
    color: var(--color-text-meta);
    display: flex;
    align-items: center;
    gap: 6px;
  }

  .card-category {
    color: var(--color-gold-dark);
    font-weight: 700;
    text-transform: uppercase;
    font-size: 0.7rem;
    letter-spacing: 0.03em;
  }

  .card-divider {
    color: var(--color-border);
  }

  .card-reading-time {
    font-size: 0.7rem;
    color: var(--color-text-meta);
  }

  /* Hero variant */
  .card--hero .card-title {
    font-size: 1.75rem;
    line-height: 1.2;
  }

  .card--hero .card-desc {
    font-size: 1rem;
    line-height: 1.6;
  }

  /* Sidebar variant — no image, compact */
  .card--sidebar {
    padding-bottom: 16px;
    margin-bottom: 16px;
    border-bottom: 1px solid var(--color-border);
  }

  .card--sidebar:last-child {
    border-bottom: none;
    margin-bottom: 0;
    padding-bottom: 0;
  }

  .card--sidebar .card-title {
    font-size: 1.05rem;
  }

  /* Compact variant — horizontal: thumbnail left, text right (BBC-style) */
  .card--compact .card-link {
    display: grid;
    grid-template-columns: 120px 1fr;
    gap: 12px;
    align-items: start;
  }

  .card--compact .card-image {
    margin-bottom: 0;
  }

  .card--compact .card-image img {
    aspect-ratio: 4 / 3;
    border-radius: 2px;
  }

  .card--compact .card-title {
    font-size: 0.95rem;
    margin-bottom: 6px;
  }

  .card--compact .card-desc {
    display: none;
  }

  .card--compact .card-meta {
    font-size: 0.7rem;
  }
</style>
